# Guessing Game

## Objective  
Build a workflow using a Do While activity for creating a 'Guessing Game' with the following conditions: 

- Generate a random number and prompt the user to input a number 

- If the input number is greater than the number generated, then it should display the message:  'Please enter a lesser number’ 

- If the input number is lesser than the number generated, then it should display the message:  'Please enter a greater number’ 

- The loop keeps on running until the input tewqnumber equals to the generated number

## Process Map
<img width="911" height="628" alt="image" src="https://github.com/user-attachments/assets/24a69012-dce7-49c2-9df6-65ade33accfc" />

## Project Information 

We use activities such as Do While and If statements to program conditionally, this allows us to change what our automation is doing based on given conditions and their status, for example in the above diagram we can see how the automation will go down different paths based both on whether the guess is correct and if the guess is higher or lower than the actual number the system has stored. 
